<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="App1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="using:App1.Views"
    xmlns:viewmodel="using:App1.ViewModels"
    mc:Ignorable="d">
    <Grid x:Name="rootGrid">
        <Grid.Resources>
            <DataTemplate x:Key="postsListTemplate" x:DataType="viewmodel:PostViewModel">
                <ItemContainer>
                    <view:PostView ViewModel="{Binding }"/>
                </ItemContainer>
            </DataTemplate>
        </Grid.Resources>
        <ItemsView x:Name="postsList" ItemsSource="{Binding Posts}" ItemTemplate="{StaticResource postsListTemplate}">
        </ItemsView>
    </Grid>
</Window>
